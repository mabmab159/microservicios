spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:9000 #Url del config server
      profile: dev # Se buscara un archivo user-service-dev.yml
      enabled: true